# Python API Plan

Planned options:
- `mkdocstrings` for integrated MkDocs API sections
- `pdoc` for quick API publication
- `Sphinx + Napoleon` for formal Python API systems

Status: planning only in Milestone 1.
