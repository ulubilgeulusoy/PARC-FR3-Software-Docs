# Standard Workflows

1. FR3 control launch workflow (Windows orchestrator -> remote tools).
2. Kinesthetic teach/gravity/playback workflow.
3. Visual servoing workflow with calibration and safety checks.
4. Experiment session workflow (combined task GUI + marker GUI + recording).
