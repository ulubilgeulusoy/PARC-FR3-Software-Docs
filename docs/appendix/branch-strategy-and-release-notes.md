# Branch Strategy and Release Notes

Observed branch families indicate environment and feature splits (Humble/Jazzy, failsafe variants, experimental branches).

Documentation policy:
- Treat `main` as default published behavior unless page explicitly states branch-specific differences.
- Add release-note entries when runtime assumptions or validated versions change.
