# Network and Permissions

Core requirements:
- Reliable direct robot networking (static addressing pattern must match site setup).
- Remote SSH access for launcher workflows where applicable.
- X11 forwarding path for remote GUI rendering (Windows + WSL + X server).
- Realtime permissions/groups and limits for low-latency control execution.
